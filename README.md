This is a Next.js web application that uses server-side rendering to load pages quickly.

1. The Header component is the top part of the website, which contains navigation and a logo:

import Header from './components/shared';

const App = () => {
  return (
    <div>
      <Header />
      <!-- ..... -->
    </div>
  );
} -->